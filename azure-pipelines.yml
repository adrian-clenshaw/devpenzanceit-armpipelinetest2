# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- main

pool:
  vmImage: ubuntu-latest

steps:
- task: AzureResourceManagerTemplateDeployment@3
  inputs:
    deploymentScope: 'Resource Group'
    azureResourceManagerConnection: 'armpipelinetest2'
    subscriptionId: '3fbc4d39-8118-464c-965a-78b84b79c161'
    action: 'Create Or Update Resource Group'
    resourceGroupName: 'rg-dev-aus-app-project1'
    location: 'Australia Southeast'
    templateLocation: 'URL of the file'
    csmFileLink: 'https://dev.azure.com/DEVPenzanceIT/_git/ARMPipelineTEST2?path=/create_web_app/azuredeploy.json'
    overrideParameters: '-projectName project1'
    deploymentMode: 'Incremental'
    deploymentName: 'deployproject1'